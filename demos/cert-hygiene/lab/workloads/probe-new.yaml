apiVersion: apps/v1
kind: Deployment
metadata:
  name: probe-new
  namespace: ${LAB_NS}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: probe-new
  template:
    metadata:
      labels:
        app: probe-new
    spec:
      containers:
        - name: idle
          image: ${IMAGE_BUSYBOX}
          command: ["sh", "-c", "while :; do sleep 3600; done"]
