# Discovery only (Task 1 Step 5): a lab-supplied tap serving certificate with a
# 24-hour lifetime, so checking that Linkerd serves it and that tap works never
# races the 15-minute tap-short certificate.
ANCHOR_LIFETIME=87600h
ISSUER_LIFETIME=8760h
LEAF_LIFETIME=5m
WEBHOOK_CERT_LIFETIMES=
EXTRA_INSTALL_FLAGS=
TAP_CERT_LIFETIME=24h
